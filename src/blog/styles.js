import { space } from '../styles/gobal'

export const styles = {
    h1: {
        marginBottom: space,
    },
    '.post-listing': {
        padding: space,
        borderBottom: '1px solid #333',
        marginBottom: space,
    }
}